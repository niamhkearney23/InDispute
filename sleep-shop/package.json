{
  "name": "hush-sleep-shop",
  "version": "1.0.0",
  "private": true,
  "description": "Hush — a static sleep shop storefront. No build step, no dependencies.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
